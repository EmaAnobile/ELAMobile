<?php

Class Model_Tableros extends Model_Abstract{


 Protected $_name = 'tableros';
Protected $_rowClass = 'Model_Row_Tablero';
 
/*
   $sql = $this->select()->where('activo = ?', 1);

            if ($incluirPendietes === false) {
                $sql->where('pendiente_revision = 0');
            } else {
                $sql->order(['pendiente_revision DESC', 'nombre']);
            }

            $rows = $rol->findDependentRowset('Model_Usuarios', null, $sql);

 protected $_referenceMap = array(
        'Usuarios' => array(
            'columns' => array('id'),
            'refTableClass' => 'Model_Usuarios',
            'refColumns' => array('rol_id')
        ),
        'Permisos' => array(
            'columns' => array('id'),
            'refTableClass' => 'Model_Permisos',
            'refColumns' => array('rol_id')
        ),
    );

*/


}